// This file contains constants related to toast notifications.
// It is used to define the default lifetime of toast notifications. (MS)
const ToastLifeTime = 3000;

const ToastConstants = {
  ToastLifeTime,
};

export default ToastConstants;
